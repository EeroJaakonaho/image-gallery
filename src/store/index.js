import Vue from "vue";
import Vuex from "vuex";
import images from "./module/images";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    images
  }
});
